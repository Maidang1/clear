## Agent skills

### Issue tracker

Issues、PRD 和 Wayfinder 决策地图存放在 `Maidang1/clear` 的 GitHub Issues。具体规则见 `docs/agents/issue-tracker.md`。

### Domain docs

Clear 使用单上下文领域模型：术语存放在根目录 `CONTEXT.md`，重大且难以逆转的决策存放在 `docs/adr/`。具体规则见 `docs/agents/domain.md`。
