# Clear

Clear 是面向开发者和进阶 Mac 用户的本地系统健康工具，先解释可观察到的状态与风险，再提供由用户确认的安全处理方式。

## Language

**健康概览**：
汇总 Clear 对当前 Mac 状态的可解释判断，并通过证据和优先级引导用户进入具体诊断发现。
_Avoid_：健康总分、一键体检

**诊断发现**：
Clear 从系统信号中识别出的一项值得用户关注的状态，包含依据、影响和可选处理建议。
_Avoid_：垃圾、问题项

**安全处理**：
由用户明确发起、已说明风险和预期影响，并提供结果与恢复路径的操作。
_Avoid_：一键优化、自动修复

**安全事务**：
跨模块统一描述一次安全处理从预览、确认和复验到结果与恢复路径的完整生命周期。
_Avoid_：清理任务、批量优化

**事务预览**：
安全事务中绑定具体目标、证据、预期影响、权限需求、风险和恢复方式的不可变快照；目标或证据变化后必须重新生成。
_Avoid_：确认弹窗、操作摘要

**目标身份**：
用于在执行前确认安全处理仍指向同一对象的一组稳定证据，例如文件与卷标识、进程标识与启动时间、登录项标识及状态版本。
_Avoid_：路径、名称

**复验**：
安全事务执行副作用前，以事务预览中的目标身份和前置条件重新观察目标；不一致时拒绝对该目标执行。
_Avoid_：刷新、二次扫描

**不确定结果**：
Clear 已尝试产生副作用，但无法可靠证明最终状态的事务或事务项；出现后必须停止扩大本次事务，并引导用户核对。
_Avoid_：失败、可能成功

**恢复路径**：
安全事务完成或部分完成后，为每个已改变目标提供的可执行恢复说明或受控撤销入口；它不是对所有操作都承诺自动回滚。
_Avoid_：撤销保证、自动回滚

**事务日志**：
安全事务为防止崩溃后误判或重放副作用而持久化的最小状态记录；它服务于 reconcile，不等同于用户可清除的处理历史。
_Avoid_：操作日志、清理历史

**处理历史**：
面向用户展示的安全事务结果摘要，可以导出或清除，但不承担未完成事务的恢复职责。
_Avoid_：事务日志、遥测

**诊断历史**：
模块状态、数据质量和诊断发现数量的有界聚合记录；不包含完整文件清单、进程清单或可重建的原始扫描结果。
_Avoid_：扫描缓存、行为数据

**关注级别**：
诊断发现基于当前可观察影响、时间敏感性和可处理性得到的离散等级，用于说明何时值得查看；它不是健康分数，也不跨模块累加。
_Avoid_：严重度分数、健康分

**数据完整性**：
一次模块判断实际覆盖的范围、新鲜度和受阻原因；它与关注级别独立表达，缺失数据不代表健康。
_Avoid_：置信分、扫描进度

**模块摘要**：
模块向健康概览提供的可解释投影，包含状态句、关注级别、关键证据、数据完整性、观测时间和一个安全下一步。
_Avoid_：模块分数、入口卡片

**呈现优先级**：
健康概览为选择默认展开模块而使用的稳定排序；它先比较已确认发现，再考虑阻止判断的数据缺口，不作为用户可见的数值。
_Avoid_：排行榜、健康排名

**清理规则**：
Clear 内置并带版本的安全扫描契约，限定一个精确根目录、候选资格、资源预算、风险和执行前置条件；普通流程不接受外部或用户临时生成的规则。
_Avoid_：清理路径、匹配模式

**可再生文件**：
有明确生产者、删除后不会删除用户创作内容且可由应用或工具重新生成的文件；重新生成可能有时间或网络成本。
_Avoid_：垃圾文件、无用文件

**系统压力事件**：
macOS 公开通知的内存压力条件变化，包含 normal、warning 或 critical 及观测时间；它不是连续百分比，首次事件前状态未知。
_Avoid_：内存压力值、Activity Monitor 压力

**内存状态估算**：
Clear 根据公开 VM 计数和相邻样本变化生成、带规则版本的辅助判断；它与系统压力事件分开呈现。
_Avoid_：系统内存压力、精确压力

**启动声明**：
可读 launchd plist 对程序、作用范围和触发条件的静态声明；它不证明项目当前已启用或正在运行。
_Avoid_：启动项状态、后台进程

**系统设置交接**：
Clear 在无法通过公开接口控制第三方系统对象时，先解释证据和数据缺口，再把用户带到 Apple 的设置界面完成操作；Clear 不声称操作已经完成。
_Avoid_：间接禁用、代操作

**应用资产**：
应用卸载分析中的一个本地对象，可能是用户明确选择的应用包，或与其存在可验证归属关系的可再生附属文件。
_Avoid_：应用残留、关联垃圾

**归属等级**：
Clear 对应用资产属于特定应用的证据强度分层；它只决定候选资格与默认选择，不因权限增加而提高，也不等同于删除许可。
_Avoid_：可删除分、扫描置信度

**菜单栏守护**：
用户可关闭的被动监控入口，用于展示状态和提醒；它不自行执行破坏性操作。
_Avoid_：后台清理、自动优化

**发布门槛**：
Clear 的某个版本可以公开发布前必须同时满足的安全、正确性、兼容性、性能和验证证据条件；任一硬性条件未满足都会阻止发布。
_Avoid_：发布建议、尽力目标

**模块完成契约**：
内存、磁盘、启动项和应用卸载模块达到稳定可交付状态时共同遵守的产品承诺，覆盖可靠观测、可解释诊断、安全处理、结果或恢复路径以及权限受限时的明确降级。
_Avoid_：页面完成、功能已接入
